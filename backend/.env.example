# AI reasoning (wired up in a later iteration)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# Path to a kubeconfig for cluster investigation (later iteration)
KUBECONFIG_PATH=

# Comma-separated list of allowed CORS origins for the frontend
CORS_ORIGINS=http://localhost:3000
